pre-commit:
  parallel: true
  commands:
    typecheck:
      run: "pnpm check-types"
    lint:
      run: "pnpm lint"
    format:
      run: "pnpm format"

commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit


